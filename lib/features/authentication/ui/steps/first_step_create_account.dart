import 'package:flutter/widgets.dart';

class FirstStepCreateAccount extends StatelessWidget {
  const FirstStepCreateAccount({super.key});

  @override
  Widget build(final BuildContext context) {
    return const Placeholder();
  }
}
