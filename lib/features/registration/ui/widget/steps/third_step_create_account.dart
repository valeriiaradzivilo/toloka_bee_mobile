import 'package:flutter/widgets.dart';

class ThirdStepCreateAccount extends StatelessWidget {
  const ThirdStepCreateAccount({super.key});

  @override
  Widget build(final BuildContext context) {
    return const Placeholder();
  }
}
